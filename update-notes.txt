Older-phone updater signer compatibility: fixes a false signature_mismatch after the v2 signing-key rotation when some Android/OEM PackageManager implementations report different certificate-history shapes for the installed app and downloaded APK. QuietLink now requires the exact pinned v2 current signer on both sides for this compatibility path; Android still enforces the signing lineage during install. v0.3.49 internet-session work is unchanged.