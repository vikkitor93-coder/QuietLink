Rotation lab UI compatibility: fixes the older Android phone showing only the 'Production defaults' explanation and no selectable rotation controls. The lab is now list-first and avoids combining AlertDialog message + item list on older platform implementations. v0.3.53 Sleeping Baby audio/role fix and v0.3.52 updater signer compatibility are user-validated.