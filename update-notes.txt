Developer video rotation lab: hidden developer tools now include live-selectable H.264 sender formulas (current/Android/WebRTC/sensor-only), display vs physical orientation source, local TextureView rotation strategy, front-preview mirroring, optional per-frame H.264 rotation metadata, manual sender/receiver rotation overrides, and live H.264/JPEG comparison. Production defaults remain unchanged until a dev setting is selected.