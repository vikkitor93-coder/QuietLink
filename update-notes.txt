Internet session checkpoint: QuietLink can now carry the existing QL5 encrypted control stream through the production rendezvous while audio/video continue over direct encrypted UDP. Local LAN/Hotspot/Wi-Fi Direct behavior remains first and unchanged. The Online status stays conservative until the cross-network call path is validated on real phones.