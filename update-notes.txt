Fourth-phone validation showed v0.3.62's canonical front-camera conversion still used the wrong quarter-turn on a fresh device. v0.3.63 introduces ROT_REL2 so two current peers preserve Android Camera2's sensor-relative rotation value directly; mixed v0.3.62/v0.3.63 peers safely fall back to the legacy profile pipeline rather than disagreeing about angle semantics. The same build also repairs CODE same-WiFi discovery with an active opaque-room UDP probe/reply path, NSD conflict-suffix tolerance and privacy-safe route diagnostics so LAN can win before the 8-second Wi-Fi Direct fallback. Portrait lock remains during validation; QL5 crypto, signing continuity and local-first behavior are unchanged.
