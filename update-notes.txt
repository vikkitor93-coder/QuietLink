Architecture hardening Phase 0 begins. Developer tools now include a safe 6-second in-call Quick App Test that samples encrypted-session health, heartbeat/RTT, recovery stability, audio/video packet flow, queues/loss, video surfaces/orientation state, Baby synchronization and diagnostic-transfer state without changing call settings. The resulting report is privacy-safe and copyable for fast bug reports. CI now runs the same pure-Java evaluator rules. ARCHITECTURE_ROADMAP.md defines the incremental plan to isolate session state, connection routing, recovery and feature controllers without a big-bang rewrite. v0.3.63 rotation/LAN fixes are retained unchanged.
