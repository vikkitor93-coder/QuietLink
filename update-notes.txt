Video rotation iteration UX: adds a compact ROTATE ONLY developer panel pinned to the bottom so the live video stays visible while testing. It exposes sender rotation, remote offset/direction, formula, preview transform, rotation source, frame TX/RX and mirror; taps apply live without closing or jumping the panel. The small self-preview now follows the local video orientation and uses fit/preserve-aspect transforms instead of crop/stretch behavior.