Portrait stabilization + Wi-Fi Direct state repair: QuietLink is temporarily locked to portrait orientation so the proven per-device video calibration profiles are not invalidated by landscape display rotation. Received chat .txt attachments now have a DOWNLOAD button that copies the cached file through Android's document picker. Wi-Fi Direct now waits for Android's P2P state to become enabled before starting group/discovery operations, queries P2P readiness directly on Android 10+, distinguishes Wi-Fi-radio-off from transient P2P startup, and avoids hammering createGroup/discovery with BUSY while the framework still reports P2P disabled. Existing saved profiles, QL5, internet CODE, and signing remain unchanged.