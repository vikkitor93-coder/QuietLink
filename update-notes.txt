Diagnostics workflow improvement: privacy-safe log + saved calibration profiles can now be exported locally as a .txt from both QuietLink chat and the Developer tools menu, without requiring an active peer transfer. Profiles can also be exported alone. The profile report includes current display/reported/resolved rotation values, current tuning, and all four saved Video/Baby Inline/Fullscreen profiles so rotation/aspect offsets can be compared directly when diagnosing the underlying camera transform issue.